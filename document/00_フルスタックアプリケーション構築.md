# Hono + React プロジェクトをゼロから作成する手順

このドキュメントでは、intuitiveプロジェクトのような**Hono (バックエンド) + React (フロントエンド)** の構成を、ゼロから作成する手順を詳しく説明します。

## 目次

1. [前提条件](#前提条件)
2. [Step 1: プロジェクトの初期化](#step-1-プロジェクトの初期化)
3. [Step 2: 依存関係のインストール](#step-2-依存関係のインストール)
4. [Step 3: TypeScript設定ファイルの作成](#step-3-typescript設定ファイルの作成)
5. [Step 4: Vite設定ファイルの作成](#step-4-vite設定ファイルの作成)
6. [Step 5: バックエンドの作成](#step-5-バックエンドの作成)
7. [Step 6: フロントエンドの作成](#step-6-フロントエンドの作成)
8. [Step 7: HTMLテンプレートの作成](#step-7-htmlテンプレートの作成)
9. [Step 8: 起動スクリプトの設定](#step-8-起動スクリプトの設定)
10. [Step 9: .gitignoreの作成](#step-9-gitignoreの作成)
11. [Step 10: 動作確認](#step-10-動作確認)

---

## 前提条件

以下がインストールされていること：

- **Bun**: JavaScriptランタイム（Node.jsの代替）
  ```bash
  # Bunのインストール（未インストールの場合）
  curl -fsSL https://bun.sh/install | bash
  ```

- **Git**: バージョン管理システム（任意）
  ```bash
  git --version
  ```

---

## Step 1: プロジェクトの初期化

### 1-1. プロジェクトディレクトリを作成

```bash
# プロジェクト用のディレクトリを作成
mkdir my-fullstack-app
cd my-fullstack-app
```

### 1-2. package.jsonを作成

```bash
# package.jsonを初期化
bun init -y
```

実行後、以下のような`package.json`が作成されます：

```json
{
  "name": "my-fullstack-app",
  "version": "1.0.0",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  }
}
```

### 1-3. プロジェクト構造を作成

```bash
# ディレクトリ構造を作成
mkdir -p src/backend
mkdir -p src/frontend
mkdir -p document
```

現在の構造：
```
my-fullstack-app/
├── src/
│   ├── backend/
│   └── frontend/
├── document/
└── package.json
```

---

## Step 2: 依存関係のインストール

### 2-1. バックエンド用の依存関係

```bash
# Honoフレームワークをインストール
bun add hono
```

### 2-2. フロントエンド用の依存関係

```bash
# React関連をインストール
bun add react react-dom

# React型定義をインストール（開発用）
bun add -d @types/react @types/react-dom
```

### 2-3. ビルドツール（Vite）のインストール

```bash
# Viteとプラグインをインストール
bun add -d vite @vitejs/plugin-react
```

### 2-4. インストール確認

`package.json`が以下のようになっているか確認：

```json
{
  "name": "my-fullstack-app",
  "dependencies": {
    "hono": "^4.11.3",
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.1.2",
    "vite": "^7.3.0"
  }
}
```

---

## Step 3: TypeScript設定ファイルの作成

### 3-1. フロントエンド用 `tsconfig.json`

プロジェクトルートに`tsconfig.json`を作成：

```json
{
  "compilerOptions": {
    "strict": true,
    "jsx": "react-jsx",
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "target": "ESNext",
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "types": ["vite/client"]
  },
  "include": ["src/frontend/**/*"],
  "exclude": ["src/backend"]
}
```

**重要なポイント**:
- `jsx: "react-jsx"` → ReactのJSXをサポート
- `lib: ["DOM", "DOM.Iterable"]` → ブラウザAPI使用可能
- `include: ["src/frontend/**/*"]` → フロントエンドのみ対象

### 3-2. バックエンド用 `tsconfig.backend.json`

プロジェクトルートに`tsconfig.backend.json`を作成：

```json
{
  "compilerOptions": {
    "strict": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "skipLibCheck": true,
    "target": "ESNext",
    "lib": ["ESNext"],
    "types": ["bun"]
  },
  "include": ["src/backend/**/*"]
}
```

**重要なポイント**:
- `lib: ["ESNext"]`のみ → DOM APIは含まない（サーバー用）
- `types: ["bun"]` → Bunランタイムの型定義
- `include: ["src/backend/**/*"]` → バックエンドのみ対象

---

## Step 4: Vite設定ファイルの作成

プロジェクトルートに`vite.config.ts`を作成：

```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    port: 8080,
    // バックエンドへのプロキシ設定
    proxy: {
      '/api': {
        target: 'http://localhost:3000',
        changeOrigin: true,
      }
    }
  }
})
```

**各設定の意味**:
- `plugins: [react()]` → ReactをViteでビルドできるようにする
- `port: 8080` → フロントエンドの開発サーバーポート
- `proxy` → `/api`で始まるリクエストをバックエンド(3000番)に転送

---

## Step 5: バックエンドの作成

### 5-1. `src/backend/index.ts`を作成

```typescript
import { Hono } from 'hono'
import { cors } from 'hono/cors'

const app = new Hono()

// CORS設定（フロントエンドからのアクセスを許可）
app.use('/*', cors())

// APIエンドポイント
app.get('/api', (c) => {
  return c.json({ message: 'Hello from Hono API!' })
})

app.get('/api/users', (c) => {
  return c.json({
    users: ['太郎', '花子', '次郎']
  })
})

// サーバー起動
const port = 3000
console.log(`🚀 Backend server is running on http://localhost:${port}`)

export default {
  port,
  fetch: app.fetch,
}
```

**各部分の説明**:

1. **Honoのインポート**
   ```typescript
   import { Hono } from 'hono'
   ```
   Honoフレームワークを使えるようにする

2. **CORS設定**
   ```typescript
   app.use('/*', cors())
   ```
   別ポート(8080)のフロントエンドからアクセスを許可

3. **APIエンドポイント**
   ```typescript
   app.get('/api', (c) => {
     return c.json({ message: 'Hello!' })
   })
   ```
   `/api`にGETリクエストが来たらJSONを返す

4. **サーバー起動設定**
   ```typescript
   export default {
     port: 3000,
     fetch: app.fetch,
   }
   ```
   Bunがこれを読み込んでポート3000で起動

---

## Step 6: フロントエンドの作成

### 6-1. `src/frontend/main.tsx`を作成

Reactのエントリーポイント：

```typescript
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './App.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
```

**やっていること**:
1. `index.html`の`<div id="root">`を取得
2. そこに`<App>`コンポーネントをレンダリング

### 6-2. `src/frontend/App.tsx`を作成

メインコンポーネント：

```typescript
import { useState } from 'react'
import './App.css'

function App() {
  const [count, setCount] = useState(0)
  const [message, setMessage] = useState('')
  const [users, setUsers] = useState<string[]>([])

  // バックエンドからメッセージを取得
  const fetchMessage = async () => {
    try {
      const response = await fetch('/api')
      const data = await response.json()
      setMessage(data.message)
    } catch (error) {
      console.error('Error fetching message:', error)
    }
  }

  // バックエンドからユーザーリストを取得
  const fetchUsers = async () => {
    try {
      const response = await fetch('/api/users')
      const data = await response.json()
      setUsers(data.users)
    } catch (error) {
      console.error('Error fetching users:', error)
    }
  }

  return (
    <div className="app">
      <h1>Vite + React + Hono</h1>

      <div className="card">
        <h2>フロントエンド機能</h2>
        <button onClick={() => setCount((count) => count + 1)}>
          カウント: {count}
        </button>
        <p>クリックしてカウントを増やしてください</p>
      </div>

      <div className="card">
        <h2>バックエンド連携</h2>
        <button onClick={fetchMessage}>
          APIからメッセージを取得
        </button>
        {message && (
          <div className="api-response">
            {message}
          </div>
        )}
      </div>

      <div className="card">
        <h2>ユーザー一覧</h2>
        <button onClick={fetchUsers}>
          ユーザーを取得
        </button>
        {users.length > 0 && (
          <ul className="users-list">
            {users.map((user, index) => (
              <li key={index}>{user}</li>
            ))}
          </ul>
        )}
      </div>
    </div>
  )
}

export default App
```

**各機能の説明**:

1. **状態管理**
   ```typescript
   const [count, setCount] = useState(0)
   ```
   Reactの`useState`でカウントを管理

2. **API呼び出し**
   ```typescript
   const response = await fetch('/api')
   ```
   `/api`にリクエスト → Viteがプロキシで`http://localhost:3000/api`に転送

3. **条件付きレンダリング**
   ```typescript
   {message && <div>{message}</div>}
   ```
   `message`が存在する場合のみ表示

### 6-3. `src/frontend/App.css`を作成

スタイルファイル：

```css
.app {
  max-width: 800px;
  margin: 0 auto;
  padding: 2rem;
  text-align: center;
}

h1 {
  color: #646cff;
  font-size: 3em;
  margin-bottom: 1rem;
}

.card {
  padding: 2rem;
  background-color: #f9f9f9;
  border-radius: 8px;
  margin: 2rem 0;
}

button {
  padding: 0.6em 1.2em;
  font-size: 1em;
  font-weight: 500;
  cursor: pointer;
  border: 1px solid #646cff;
  background-color: #646cff;
  color: white;
  border-radius: 8px;
  transition: all 0.3s;
}

button:hover {
  background-color: #535bf2;
  border-color: #535bf2;
}

button:active {
  transform: scale(0.98);
}

.users-list {
  margin-top: 1rem;
  list-style: none;
  padding: 0;
}

.users-list li {
  padding: 0.5rem;
  background-color: #e0e0e0;
  margin: 0.5rem 0;
  border-radius: 4px;
}

.api-response {
  margin-top: 1rem;
  padding: 1rem;
  background-color: #e8f5e9;
  border-radius: 4px;
  font-family: monospace;
}
```

---

## Step 7: HTMLテンプレートの作成

プロジェクトルートに`index.html`を作成：

```html
<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Vite + React + Hono</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/frontend/main.tsx"></script>
  </body>
</html>
```

**重要なポイント**:
- `<div id="root"></div>` → Reactがここにレンダリング
- `<script src="/src/frontend/main.tsx">` → Viteが自動でトランスパイル

---

## Step 8: 起動スクリプトの設定

`package.json`の`scripts`セクションを編集：

```json
{
  "scripts": {
    "dev": "bun run dev:backend & bun run dev:frontend",
    "dev:backend": "bun run --hot src/backend/index.ts",
    "dev:frontend": "vite",
    "build": "vite build",
    "preview": "vite preview"
  }
}
```

**各スクリプトの説明**:

| コマンド | 説明 |
|---------|------|
| `bun run dev` | バックエンドとフロントエンドを同時起動 |
| `bun run dev:backend` | バックエンドのみ起動（ポート3000） |
| `bun run dev:frontend` | フロントエンドのみ起動（ポート8080） |
| `bun run build` | フロントエンドを本番用にビルド |
| `bun run preview` | ビルド結果をプレビュー |

**`&`の意味**:
```bash
command1 & command2
```
`&`で複数のコマンドを**並列実行**（同時に起動）

---

## Step 9: .gitignoreの作成

プロジェクトルートに`.gitignore`を作成：

```
# 依存関係
node_modules/

# ビルド出力
dist/
dist-ssr/
*.local

# ログ
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# エディタ
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# 環境変数
.env
.env.local
.env.*.local
```

**なぜ必要？**:
- `node_modules/`は容量が大きいのでGitで管理しない
- ビルド結果(`dist/`)もGitで管理不要
- 秘密情報(`.env`)を誤ってコミットしないため

---

## Step 10: 動作確認

### 10-1. 開発サーバーを起動

```bash
bun run dev
```

以下のように表示されればOK：

```
🚀 Backend server is running on http://localhost:3000

  VITE v7.3.0  ready in 500 ms

  ➜  Local:   http://localhost:8080/
  ➜  Network: use --host to expose
```

### 10-2. ブラウザで確認

ブラウザで以下にアクセス：

```
http://localhost:8080
```

### 10-3. 動作テスト

1. **フロントエンド機能**
   - 「カウント」ボタンをクリック
   - 数字が増えればOK

2. **バックエンド連携**
   - 「APIからメッセージを取得」ボタンをクリック
   - 「Hello from Hono API!」が表示されればOK

3. **ユーザー一覧**
   - 「ユーザーを取得」ボタンをクリック
   - 太郎、花子、次郎が表示されればOK

### 10-4. エラーが出た場合

#### ポートが既に使用されている

```
Error: listen EADDRINUSE: address already in use :::3000
```

**解決方法**:
- 別のアプリケーションがポート3000または8080を使用中
- そのアプリケーションを終了するか、ポート番号を変更

#### モジュールが見つからない

```
Error: Cannot find module 'react'
```

**解決方法**:
```bash
bun install
```

---

## 完成したプロジェクト構造

```
my-fullstack-app/
├── src/
│   ├── backend/
│   │   └── index.ts              # Honoバックエンド
│   └── frontend/
│       ├── App.tsx               # Reactメインコンポーネント
│       ├── App.css               # スタイル
│       └── main.tsx              # Reactエントリーポイント
│
├── document/
│   └── setup-from-scratch.md    # このファイル
│
├── index.html                    # HTMLテンプレート
├── package.json                  # 依存関係とスクリプト
├── bun.lock                      # ロックファイル
│
├── tsconfig.json                 # フロントエンド用TS設定
├── tsconfig.backend.json         # バックエンド用TS設定
├── vite.config.ts                # Vite設定
│
└── .gitignore                    # Git無視設定
```

---

## 次のステップ

### 機能を追加する

1. **新しいAPIエンドポイント**
   ```typescript
   // src/backend/index.ts
   app.post('/api/create', async (c) => {
     const body = await c.req.json()
     return c.json({ success: true, data: body })
   })
   ```

2. **新しいReactコンポーネント**
   ```typescript
   // src/frontend/components/MyComponent.tsx
   export function MyComponent() {
     return <div>新しいコンポーネント</div>
   }
   ```

### データベースを追加する

```bash
# Prisma（ORMツール）をインストール
bun add prisma @prisma/client
bun add -d @types/prisma
```

### 本番環境へのデプロイ

1. **フロントエンドをビルド**
   ```bash
   bun run build
   ```

2. **バックエンドをデプロイ**
   - Vercel、Railway、Flyなどにデプロイ

---

## トラブルシューティング

### Viteが起動しない

**確認事項**:
1. `@vitejs/plugin-react`がインストールされているか
2. `vite.config.ts`が正しく作成されているか
3. `index.html`がプロジェクトルートにあるか

### CORSエラーが発生する

**原因**: バックエンドにCORS設定がない

**解決方法**:
```typescript
// src/backend/index.ts
import { cors } from 'hono/cors'
app.use('/*', cors())
```

### TypeScriptエラーが出る

**原因**: tsconfig.jsonの設定が不適切

**解決方法**:
1. `tsconfig.json`と`tsconfig.backend.json`を確認
2. `include`と`exclude`が正しく設定されているか確認

---

## まとめ

この手順で以下が実現できました：

✅ Honoでバックエンド（ポート3000）
✅ ReactとViteでフロントエンド（ポート8080）
✅ Viteのプロキシで両者を連携
✅ 1つのコマンドで両方を起動
✅ TypeScriptで型安全な開発

---

次のドキュメント: [リポジトリ構造の詳細説明](./repository-structure.md)
